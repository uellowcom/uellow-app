# تقرير Uellow — المطلوب منك (إصدار التطبيق v2.2.46)

**التاريخ:** 2026-06-12
**الحالة العامة:** كل التعديلات (١٥ بند في الدفعتين) منشورة ومتحقَّق منها على السيرفر، وإصدار التطبيق **v2.2.46** مرفوع على GitHub.
**يتبقّى منك بندان فقط** لأنهما يحتاجان دخولاً لحسابات لا أملكه (Firebase / Apple / Anthropic).

---

## 🔴 البند ١ — تفعيل إشعارات iOS (الكود جاهز، ينقص خطوتان منك)

**المشكلة:** تطبيق الآيفون لا يستقبل أي إشعار (أندرويد يعمل). السبب الجذري: ملف `GoogleService-Info.plist` غير موجود لنسخة iOS، فلا يُولَّد توكن إشعارات على الآيفون أصلاً — وهذا أيضاً سبب عدم وصول إشعارات نقاط البيع (POS) لجهازك.

**ما عليك فعله (في Firebase Console — مشروع `uellow-app`):**

1. **إضافة تطبيق iOS:**
   - افتح Firebase Console ← Project Settings ← قسم *Your apps* ← **Add app ← iOS**.
   - Bundle ID: `com.uellow.app`
   - نزّل ملف **`GoogleService-Info.plist`** الناتج.
   - ضعه داخل مجلّد `ios/Runner/` في مشروع التطبيق وأضفه لهدف Runner في Xcode (أو احقنه في Codemagic CI).

2. **رفع مفتاح APNs (لتسليم الإشعار فعلياً للآيفون):**
   - Firebase Console ← Project Settings ← **Cloud Messaging**.
   - تحت *Apple app configuration* ← **Upload** مفتاح **APNs Auth Key (.p8)**.
   - أدخِل معه **Key ID** و **Apple Team ID**.
   - (المفتاح .p8 يُنشأ من Apple Developer ← Keys ← مفتاح بصلاحية *Apple Push Notifications service*.)

> بمجرّد إنجاز هاتين الخطوتين، تعمل إشعارات iOS تلقائياً (الكود الباقي منشور في v2.2.46)، وتصلك إشعارات POS على الآيفون أيضاً.

---

## 🔴 البند ٢ — شحن رصيد Anthropic (يخصّ مساعد بينا الذكي)

**المشكلة:** واجهة Anthropic API ترجع `credit balance is too low`. هذا يعني:
- **محادثة بينا مع العملاء غالباً متوقّفة حالياً** (كل ردّ يستدعي نفس الـAPI).
- مُنِع تقطير دروس الكوتش إلى قواعد مختصرة (تجاوزتها مؤقتاً بحقن أحدث الدروس مباشرة).

**ما عليك فعله:**
- ادخل حساب **Anthropic Console** ← **Plans & Billing** ← اشحن رصيداً (Credits).
- أبلِغني بعد الشحن لأقوم بتقطير دروس الكوتش إلى ٤٠ قاعدة مختصرة بضغطة واحدة.

---

## ✅ ما تم إنجازه ونشره (لا يحتاج منك شيئاً)

**الدفعة ١ (٦ بنود):** أيقونة شركة التوصيل على الخريطة · ترجمة رسالة Notify-me · إصلاح ظل وصف المنتج (أبيض نظيف) · زر التحديث على iOS يفتح App Store · زر «عرض الكل» في الشوب · كي‑نت وتالي للكويت فقط + ٤ إعدادات في الباك إند.

**الدفعة ٢ (٩ بنود):** تجهيز كود إشعارات iOS (البند ١) · إصلاح بطء/ثقل الشوب والفلاش سيل **+ إصلاح خطأ ٥٠٠ كان يمنع تحميل الفلاش** · بلوك الأقساط الاحترافي الجديد (٥٨٦ منتج) + زر «المزيد» · إصلاح اكسبلور‑مور الفارغ · إشعار للأدمن عند أي عملية POS · إحصائيات «الآن على التطبيق» في شاشة الأدمن · زر Approve‑selected في Coach · بنر تحميل التطبيق يظهر الآن على الكمبيوتر · بوب‑أب تحميل التطبيق + إعداداته في الثيم.

**تحديث بينا:** صارت تأخذ **أحدث** دروس الكوتش (كانت تأخذ الأقدم فقط) + أُضيفت حقائق المنصّة (الأقساط، كي‑نت/تالي للكويت).

**إصدار التطبيق:** [v2.2.46 على GitHub](https://github.com/uellowcom/uellow-app/releases/tag/v2.2.46) — حزمة AAB موقّعة للـPlay + ملفات APK للتنزيل المباشر.
