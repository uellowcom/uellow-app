// Auto-bumped on every release alongside pubspec.yaml — used by the
// in-app update gate to compare against the backend's min_version.
// MUST stay in sync with pubspec version (the update gate sends THIS, not
// the real package version) — a stale value = endless force-update loop.
const String kAppVersion = '2.2.44';
